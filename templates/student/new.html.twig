{% extends 'base.html.twig' %}

{% block body %}

  <h2>Ajout d'un étudiant</h2>

  {# {{ form(form) }} #}

  {{ form_start(form) }}
    
   <div class="row job">
    {{ form_row(form.job) }}
   </div>

   <div class="age">
    {# form_row = form_label + form_widget + form_errors #}
    <span style="color: orange">{{ form_label(form.age) }}</span>
    {{ form_widget(form.age) }}
      <section class="errors">
      {{ form_errors(form.age) }}
      </Section>
   </div>


  {{ form_end(form) }}

{% endblock %}